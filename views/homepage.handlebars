<h4 class="align-center">Tech Talk</h4>
<h6 class="align-center">See what others are saying</h6>

{{!-- <ul class="article-ul">
    {{#each posts}}
    <div>
        <a href="/{{id}}">
        <h4>{{title}}</h4>
        </a>
        <p>{{text}}</p>
        <p>By: {{user.userName}}</p>
    </div>
    {{/each}}

</ul> --}}

<ul class = "article-ul">
    {{#each posts}}
  <div class="row">
    <div class="col s12 m6">
      <div class="card">
        <div class="card-content">
          <span class="card-title">{{title}}</span>
          <p class="card-text">{{text}}</p>
          <p class="by-line">By: {{user.userName}}</p>
          <a href="/{{id}}">View Post</a>
        </div>
      </div>
    </div>
  </div>
  {{/each}}
  </ul>